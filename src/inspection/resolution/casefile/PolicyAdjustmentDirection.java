package inspection.resolution.casefile;

/** Universal direction of a recommended deployment policy adjustment. */
public enum PolicyAdjustmentDirection {
    RELAX,
    RESTRICT,
    RECONFIGURE,
    CREATE,
    SUSPEND,
    REMOVE
}
