package inspection.resolution.casefile;

/**
 * Economic region on which a concrete SEV deployment should intervene.
 * This is scope, not moral gravity: the observed anomaly and the chosen
 * intervention may legitimately belong to different institutional regions.
 */
public enum InstitutionalActionScope {
    CONSUMABLE,
    ACCOUNT,
    GROUP,
    PROFESSION,
    SYSTEM
}
