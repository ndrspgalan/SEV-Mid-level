package inspection.investigation.casefile;

/** Inspector's current conclusion after contrasting the recorded evidence. */
public enum HypothesisRecordStatus {
    OPEN,
    SUPPORTED,
    REFUTED,
    INCONCLUSIVE
}
