package inspection.doctrine.casefile;

/** Records whether seasonal doctrine arose from prevailing jurisprudence or forced resolution mode. */
public enum DoctrineUnificationMethod {
    DOMINANT_AGREEMENT,
    FORCED_MODE
}
