package economicEvent;

/** Coarse analytical grouping of economic event types. */
public enum EconomicEventCategory {
    MONETARY,
    COMMERCIAL,
    TRANSFER,
    INSTITUTIONAL,
    LIFECYCLE,
    OPERATIONAL_CONTROL
}
