package behavior.correlation.profile;

/** Descriptive displacement from the declared profile to a compatible profile. */
public enum AlignmentDisplacementDirection {
    NATIVE,
    LATERAL,
    UPWARD,
    DOWNWARD,
    CROSS_PROFILE
}
